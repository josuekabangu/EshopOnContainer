apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Ordering-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Ordering-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Ordering-chart.fullname" . }}:{{ .Values.servicePort}}']
  restartPolicy: Never
