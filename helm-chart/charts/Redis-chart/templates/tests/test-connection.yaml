apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Redis-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Redis-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Redis-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
