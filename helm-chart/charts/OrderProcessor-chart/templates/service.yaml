apiVersion: v1
kind: Service
metadata:
  name: {{ include "OrderProcessor-chart.fullname" . }}
  namespace: {{ .Values.global.namespace | default "dev" }}
  labels:
    {{- include "OrderProcessor-chart.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "OrderProcessor-chart.selectorLabels" . | nindent 4 }}
