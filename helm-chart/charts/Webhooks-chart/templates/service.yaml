apiVersion: v1
kind: Service
metadata:
  name: {{ include "Webhooks-chart.fullname" . }}
  namespace: {{ .Values.global.namespace | default "dev" }}
  labels:
    {{- include "Webhooks-chart.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.servicePort }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "Webhooks-chart.selectorLabels" . | nindent 4 }}
