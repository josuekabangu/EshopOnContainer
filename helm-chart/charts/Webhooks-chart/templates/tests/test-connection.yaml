apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Webhooks-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Webhooks-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Webhooks-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
