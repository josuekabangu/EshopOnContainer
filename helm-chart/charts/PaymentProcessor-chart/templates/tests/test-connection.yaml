apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PaymentProcessor-chart.fullname" . }}-test-connection"
  labels:
    {{- include "PaymentProcessor-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PaymentProcessor-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
