apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "RabbitMQ-chart.fullname" . }}-test-connection"
  labels:
    {{- include "RabbitMQ-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "RabbitMQ-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
