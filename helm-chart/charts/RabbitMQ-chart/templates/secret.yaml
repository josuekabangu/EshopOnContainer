apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
  namespace: {{ .Values.global.namespace | default "dev" }}
type: Opaque
data:
  RABBITMQ_DEFAULT_PASS: {{ .Values.secret.RABBITMQ_DEFAULT_PASS | default "" | b64enc | quote }}
