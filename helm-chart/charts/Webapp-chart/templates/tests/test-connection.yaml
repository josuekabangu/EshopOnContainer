apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Webapp-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Webapp-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Webapp-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
