apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "eshop.basket.fullname" . }}-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.basket.labels" . | nindent 4 }}
data:
  appsettings.json: |
    {
      "Redis": {
        "Host": "{{ .Values.redis.host }}",
        "Port": "{{ .Values.redis.port }}"
      }
    }
