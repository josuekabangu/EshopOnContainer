apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CatalogDB.fullname" . }}-test-connection"
  labels:
    {{- include "CatalogDB.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CatalogDB.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
