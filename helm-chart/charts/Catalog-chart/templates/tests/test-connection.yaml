apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Catalog-chart.fullname" . }}-test-connection"
  labels:
    {{- include "Catalog-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Catalog-chart.fullname" . }}:{{ .Values.servicePort }}']
  restartPolicy: Never
