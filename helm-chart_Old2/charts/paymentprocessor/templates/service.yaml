apiVersion: v1
kind: Service
metadata:
  name: {{ include "eshop.paymentprocessor.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.paymentprocessor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP konkurs
      name: http
  selector:
    {{- include "eshop.paymentprocessor.selectorLabels" . | nindent 4 }}
