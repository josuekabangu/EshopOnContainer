apiVersion: v1
kind: Pod
metadata:
  name: {{ include "eshop.ordering.fullname" . }}-test-connection
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.ordering.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: test-connection
      image: busybox
      command: ['sh', '-c', 'wget -q --spider http://{{ include "eshop.ordering.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
