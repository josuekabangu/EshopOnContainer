apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eshop.webhooks.fullname" . }}-secret
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.webhooks.labels" . | nindent 4 }}
type: Opaque
stringData:
  username: {{ .Values.database.username }}
  password: {{ .Values.database.password }}
