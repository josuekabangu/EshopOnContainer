apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eshop.webapp.fullname" . }}-secret
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.webapp.labels" . | nindent 4 }}
type: Opaque
stringData:
  apiKey: {{ .Values.apiKey }}
