apiVersion: v1
kind: Service
metadata:
  name: {{ include "eshop.mobilebff.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.mobilebff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    {{- include "eshop.mobilebff.selectorLabels" . | nindent 4 }}
