apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "eshop.mobilebff.fullname" . }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.mobilebff.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicas }}
  selector:
    matchLabels:
      {{- include "eshop.mobilebff.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "eshop.mobilebff.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "80"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: mobilebff
          image: "{{ .Values.global.registry }}{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          env:
            - name: ApiKey
              valueFrom:
                secretKeyRef:
                  name: {{ include "eshop.mobilebff.fullname" . }}-secret
                  key: apiKey
          volumeMounts:
            - name: config-volume
              mountPath: "/app/config/appsettings.json"
              subPath: appsettings.json
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: config-volume
          configMap:
            name: {{ include "eshop.mobilebff.fullname" . }}-config
