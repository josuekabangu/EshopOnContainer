apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eshop.mobilebff.fullname" . }}-secret
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.mobilebff.labels" . | nindent 4 }}
type: Opaque
stringData:
  apiKey: {{ .Values.apiKey }}
