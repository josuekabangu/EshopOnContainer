# kubernetes/Basket-API/basket-api-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: basket-api
  namespace: dev
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: basket-api
  type: ClusterIP
