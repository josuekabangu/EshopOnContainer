# kubernetes-manifests/catalogdb-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: catalogdb
spec:
  clusterIP: None  # Headless service pour StatefulSet
  selector:
    app: catalogdb
  ports:
    - port: 5432
      targetPort: 5432
